# ===== 数据：体积大，不进 git（见 PROGRESS.md 约定）=====
# 注意：只排除仓库根的 data/（不能写裸 `data/`，会连 src/wildspatial/data 源码一起排除）
/data/*
!/data/gz_models/

# ===== Python 缓存 / 字节码 =====
__pycache__/
*.pyc
*.pyo

# ===== 环境与内部缓存 =====
env/
.venv/
venv/
.codebuddy/

# ===== 系统 / 编辑器 =====
.DS_Store
.vscode/
*.swp

# ===== 大归档文件 =====
*.tgz
*.zip

# ===== 大体积中间产物（可重新生成；对应的 figs/metrics 仍进仓库）=====
experiments/M2_recon/mesh.ply
*.ply
# GIF/MP4 的中间帧（最终 GIF/MP4 与 metrics.json 进仓库，帧可重新生成）
experiments/**/gif_frames/
experiments/**/fpv_frames/
experiments/**/hud_frames/
experiments/**/pal_frames/

# Gazebo FPV 运行时生成的临时文件（可由脚本重新生成）
experiments/P2_delivery_fpv_gazebo/_capture.py
experiments/P2_delivery_fpv_gazebo/_delivery_fpv_world.sdf
experiments/P2_delivery_fpv_gazebo/gz.log
experiments/P2_delivery_fpv_gazebo/tb3.log

# 动态障碍实验的探测临时世界
experiments/P2_dynamic_obstacle/_probe_world.sdf

# ===== 说明 =====
# experiments/ 内的 figs/ 与 metrics.json 会随代码一起提交，
# 因为 docs/*.md 用相对路径引用这些图，必须进仓库才能在 GitHub 上正常显示。
